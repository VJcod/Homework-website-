# 🔧 Настройка Supabase для страницы измерений

Этот файл содержит пошаговую инструкцию по настройке базы данных Supabase для работы со страницей измерений параметров аквариума.

## 📋 Что нужно для подключения

Для подключения к Supabase вам потребуются **2 параметра**:

1. **SUPABASE_URL** - URL вашего проекта
2. **SUPABASE_ANON_KEY** - Публичный ключ (anon/public key)

---

## 🚀 Шаг 1: Создание проекта в Supabase

### 1.1. Регистрация/Вход

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Нажмите **"Start your project"** или **"Sign in"**
3. Войдите через GitHub или создайте аккаунт

### 1.2. Создание нового проекта

1. Нажмите **"New project"**
2. Выберите организацию или создайте новую
3. Заполните форму:
   - **Name**: Введите название проекта (например, "Aquarium Tracker")
   - **Database Password**: Придумайте надежный пароль (сохраните его!)
   - **Region**: Выберите регион ближе к вам (Europe West для России)
   - **Pricing Plan**: Выберите "Free" для начала
4. Нажмите **"Create new project"**
5. Подождите ~2 минуты пока проект создается

---

## 🗄️ Шаг 2: Создание таблицы

### 2.1. Открыть SQL Editor

1. В левом меню выберите **"SQL Editor"** (иконка </> )
2. Нажмите **"New query"**

### 2.2. Выполнить SQL запрос

Скопируйте и вставьте этот SQL код:

```sql
CREATE TABLE aquarium_measurements (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    measurement_datetime TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    temperature NUMERIC(4, 1),
    ph NUMERIC(3, 1),
    general_hardness NUMERIC(5, 1),
    carbonate_hardness NUMERIC(5, 1),
    ammonia NUMERIC(5, 2),
    nitrites NUMERIC(5, 2),
    nitrates NUMERIC(6, 2),
    phosphates NUMERIC(5, 2),
    co2 NUMERIC(5, 1),
    calcium NUMERIC(6, 1),
    iron NUMERIC(5, 3),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_measurement_datetime ON aquarium_measurements(measurement_datetime DESC);

COMMENT ON TABLE aquarium_measurements IS 'Таблица для хранения измерений параметров воды в аквариуме';
```

3. Нажмите **"Run"** или используйте **Ctrl+Enter**
4. Должно появиться сообщение **"Success. No rows returned"**

---

## 🔐 Шаг 3: Настройка безопасности (Row Level Security)

### 3.1. Включить RLS

1. Перейдите в **"Table Editor"** в левом меню
2. Выберите таблицу **aquarium_measurements**
3. Нажмите на **"RLS"** (Row Level Security)
4. Включите RLS, если он выключен

### 3.2. Создать политики доступа

Вернитесь в **SQL Editor** и выполните следующие запросы:

```sql
-- Разрешить чтение всем пользователям
CREATE POLICY "Enable read access for all users"
ON "public"."aquarium_measurements"
AS PERMISSIVE FOR SELECT
TO public
USING (true);

-- Разрешить вставку всем пользователям
CREATE POLICY "Enable insert for all users"
ON "public"."aquarium_measurements"
AS PERMISSIVE FOR INSERT
TO public
WITH CHECK (true);

-- Разрешить удаление всем пользователям
CREATE POLICY "Enable delete for all users"
ON "public"."aquarium_measurements"
AS PERMISSIVE FOR DELETE
TO public
USING (true);
```

**⚠️ Важно**: Эти политики дают доступ всем пользователям. Для публичного приложения рекомендуется настроить аутентификацию.

---

## 🔑 Шаг 4: Получение ключей доступа

### 4.1. Открыть настройки API

1. В левом меню нажмите **⚙️ Settings**
2. Выберите **"API"**

### 4.2. Скопировать данные

Вы увидите два важных параметра:

#### Project URL
```
https://xyzcompany.supabase.co
```
☝️ Это ваш **SUPABASE_URL**

#### Project API keys

Найдите раздел "anon public":
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...
```
☝️ Это ваш **SUPABASE_ANON_KEY**

---

## ⚙️ Шаг 5: Настройка config.js

### 5.1. Открыть файл config.js

Откройте файл `config.js` в корне вашего проекта

### 5.2. Вставить ваши данные

Замените значения:

```javascript
// БЫЛО:
const SUPABASE_URL = 'YOUR_SUPABASE_URL_HERE';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY_HERE';

// СТАЛО (ваши реальные данные):
const SUPABASE_URL = 'https://xyzcompany.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

### 5.3. Сохранить файл

Сохраните изменения (**Ctrl+S** или **Cmd+S**)

---

## ✅ Шаг 6: Проверка подключения

### 6.1. Открыть страницу measurements.html

Откройте `measurements.html` в браузере

### 6.2. Проверить консоль

1. Откройте консоль разработчика (**F12** или **Cmd+Option+I**)
2. Перейдите на вкладку **Console**
3. Должно появиться сообщение:
   ```
   ✅ Подключение к Supabase успешно установлено
   ```

### 6.3. Проверить работу

1. Заполните форму с измерениями
2. Нажмите **"Сохранить запись"**
3. Должно появиться уведомление: **"✅ Измерение успешно сохранено!"**
4. Нажмите **"Посмотреть историю"**
5. Вы должны увидеть сохраненное измерение в таблице

---

## 🎉 Готово!

Теперь ваше приложение подключено к Supabase и готово к работе!

---

## 🔍 Устранение проблем

### Ошибка: "Failed to fetch"

**Причина**: Неправильный URL или проблемы с сетью

**Решение**:
- Проверьте, что URL скопирован полностью
- Проверьте интернет-соединение
- Убедитесь, что проект активен в Supabase Dashboard

### Ошибка: "Invalid API key"

**Причина**: Неправильный ключ

**Решение**:
- Убедитесь, что скопировали **anon public** ключ, а не service_role
- Проверьте, что ключ скопирован полностью без пробелов

### Ошибка: "new row violates row-level security policy"

**Причина**: Не настроены политики RLS

**Решение**:
- Выполните SQL запросы из Шага 3.2
- Убедитесь, что RLS включен для таблицы

### Данные не сохраняются

**Решение**:
1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок
3. Убедитесь, что таблица создана: Supabase → Table Editor
4. Проверьте политики безопасности: Supabase → Authentication → Policies

---

## 📚 Дополнительные ресурсы

- [Документация Supabase](https://supabase.com/docs)
- [Supabase JavaScript Client](https://supabase.com/docs/reference/javascript/introduction)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

## 🆘 Нужна помощь?

Если у вас возникли проблемы:

1. Проверьте все шаги еще раз
2. Посмотрите консоль браузера на наличие ошибок
3. Проверьте, что проект активен в Supabase Dashboard
4. Обратитесь к документации Supabase

---

**Удачи с отслеживанием параметров вашего аквариума! 🐠**
